import { AddMerchantComponent } from "./add-merchant/add-merchant.component";
import { Routes } from "@angular/router";

export const routes: Routes = [
    { path: 'add_merchant', component: AddMerchantComponent }
]